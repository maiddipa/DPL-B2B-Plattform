import { Address } from 'apps/dpl-live/src/app/core/services/dpl-api-services';

/**
 * A factory function that creates Addresses
 */
export function createAddress(params: Partial<Address>) {
  return {} as Address;
}
